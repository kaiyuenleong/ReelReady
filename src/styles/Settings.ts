import { StyleSheet } from "react-native";

export default StyleSheet.create({
  contentContainer: {
    paddingLeft: 30,
    paddingRight: 30,
    paddingTop: 30,
    paddingBottom: 30,
    flex: 1
  }
});